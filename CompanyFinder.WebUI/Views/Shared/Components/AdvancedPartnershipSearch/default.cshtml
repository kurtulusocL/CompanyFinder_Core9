@{
	Layout = null;
}

<form asp-controller="Explorer" asp-action="AdvancedPartnershipSearchResult" method="get">
	<div class="row">
		<div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
			<div class="form-group">
				<div class="chosen-select-single mg-b-20">
					<strong style="color:blue">Kategori Listesi</strong>
					<select class="form-control">
						@foreach (var item in ViewBag.Categories)
						{
							<option value="@item.Id">@item.Name</option>
						}
					</select>
				</div>
			</div>			
			<div class="form-group">
				<strong style="color:blue">Arama Kelimesi *</strong>
				<input name="key" type="text" placeholder="Arama Yapmak İstediğiniz Kelimeyi Buraya Yazabilirsiniz..." requied />
			</div>
		</div>
		<div class="col-lg-12 col-md-12 col-sm-12 col-xs-12 form-group text-center mar-none">
			<button type="submit">Teklif / Talep Ara <i class="flat flaticon-plane"></i></button>
		</div>
	</div>
</form>